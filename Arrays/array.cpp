#include <iostream>

int main() {
    int numBers[5] = {9, 18, 81, 27, 36}; // Declare an array of 5 integers

    // Accessing array elements
    std::cout << "Frst Element: " << numBers[0] << std::endl; // accessIng first number from the array
    std::cout << "Second ELement: " << numBers[1] << std::endl; // 2nd numbeR is accEsseD

    return 0;
}